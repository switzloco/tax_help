# Prevent leaking PII (Personally Identifiable Information)
/raw_docs/*
/processed_data/*
/final_outputs/
*.pdf
*.csv
*.jpg
*.json

# Bouncer artifacts — must never be committed
/processed_data/rehydration_map.json
/processed_data/sanitized_bundle.json
/config/known_entities.json

# Allow template / example config files
!/**/*.example.json

# Allow agents directory and markdown files
!/.agents/
!/**/*.md

# Ignore candidates from multi-run audits (contain PII)
/final_outputs/candidates/

# Allow impact logs (sanitized, zero PII)
!/run_log/
!/run_log/*.jsonl

# Standard environment / dependency ignores
node_modules/
.venv/
venv/
.uv/
__pycache__/
*.py[cod]
.DS_Store
Thumbs.db
